# `artifacts/` (generated, local)

`collect_artifacts.py` copies manuscript figure/table assets here for local inspection.

- This directory is intentionally gitignored.
- Recreate contents with:

```bash
make collect
```
